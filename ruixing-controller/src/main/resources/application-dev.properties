debug=false
#---------server setting-----------------
server.port=8090
server.servlet.context-path=/ruixing
#---------logging setting-----------------
# <!-- 日志级别排序为： TRACE < DEBUG < INFO < WARN < ERROR -->
#---------datasource setting--------------
spring.datasource.username=ruixing
spring.datasource.password=FahdTPbPKR6GWGEC
spring.datasource.url=jdbc:mysql://139.196.93.171/ruixing?serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.username=ruixing
#spring.datasource.password=FahdTPbPKR6GWGEC
#spring.datasource.url=jdbc:mysql://139.196.93.171:3306/ruixing?serverTimezone=UTC
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
#默认值0。初始化时建立物理连接的个数，初始化发生在显示调用init方法，或者第一次getConnection时。
spring.datasource.druid.initialSize=5
#默认值8。最大连接池数量。
spring.datasource.druid.maxActive=20
#spring.datasource.druid.maxIdle: 已经不再使用，配置了也没效果，可忽略。说明：maxIdle是Druid为了方便DBCP用户迁移而增加的，maxIdle是一个混乱的概念。连接池只应该有maxPoolSize和minPoolSize，druid只保留了maxActive和minIdle，分别相当于maxPoolSize和minPoolSize。
#最小连接池数量。
spring.datasource.druid.minIdle=5
#获取连接时最大等待时间，单位毫秒。配置了maxWait之后，缺省启用公平锁，并发效率会有所下降，如果需要可以通过配置useUnfairLock属性为true来使用非公平锁。
spring.datasource.druid.maxWait=60000
#默认值为false。是否缓存preparedStatement，也就是PSCache。PSCache对支持游标的数据库性能提升巨大，比如说oracle。在mysql下建议关闭。
spring.datasource.druid.poolPreparedStatements=true
#默认值-1。要启用PSCache，必须配置大于0，当大于0时，poolPreparedStatements自动触发修改为true。在Druid中，不会存在Oracle下PSCache占用内存过多的问题，可以把这个数值配置大一些，比如说100。
spring.datasource.druid.maxPoolPreparedStatementPerConnectionSize=20
#用来检测连接是否有效的sql，要求是一个查询语句，常用select ‘X’。如果validationQuery为null，testOnBorrow、testOnReturn、testWhileIdle都不会起作用。
spring.datasource.druid.validationQuery=SELECT 1 FROM DUAL
#validationQueryTimeout:单位：秒，检测连接是否有效的超时时间。底层调用jdbc Statement对象的void setQueryTimeout(int seconds)方法
#默认值为true。申请连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能。
spring.datasource.druid.testOnBorrow=false
#默认值为false。归还连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能。
spring.datasource.druid.testOnReturn=false
#默认值为false。建议配置为true，不影响性能，并且保证安全性。申请连接的时候检测，如果空闲时间大于timeBetweenEvictionRunsMillis，执行validationQuery检测连接是否有效。
spring.datasource.druid.testWhileIdle=true
#1.0.14版本，默认值为1分钟，单位毫秒。有两个含义：一个是Destroy线程会检测连接的间隔时间，如果连接空闲时间大于等于minEvictableIdleTimeMillis则关闭物理连接；另一个是testWhileIdle的判断依据，详细看testWhileIdle属性的说明。
spring.datasource.druid.timeBetweenEvictionRunsMillis=60000
#连接保持空闲而不被驱逐的最小时间，单位毫秒。
spring.datasource.druid.minEvictableIdleTimeMillis=00000
# 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙属性类型是字符串，通过别名的方式配置扩展插件，常用的插件有：监控统计用的filter:stat、日志用的filter:log4j、防御sql注入的filter:wall。
spring.datasource.druid.filters=stat,wall,log4j
spring.datasource.druid.useGlobalDataSourceStat=true
spring.datasource.druid.connectionProperties=druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
#-----------mybatis setting--------------------
mybatis.type-aliases-package=com.yintu.ruixing.entity
#mybatis.config-location=mybatis-config.xml


#pagehelper��ҳ����

pagehelper.helper-dialect=mysql
pagehelper.reasonable=true;
pagehelper.support-methods-arguments=true;
pagehelper.params=count=countSql;

















